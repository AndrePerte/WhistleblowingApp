@{
    ViewData["Title"] = "Whistleblowing - Segnalazione condotte illecite";
}

<main>
    <p class="intro">
        Il D.Lgs. n. 24/2023 ha introdotto un complesso di prescrizioni e regole volte a promuovere l’adozione ed attuazione di un adeguato sistema di gestione delle segnalazioni di condotte indebite, con l’obiettivo di favorire l’emersione di illeciti o comunque di situazioni di rischio.
    </p>

    <a asp-controller="Segnalazione" asp-action="Create" class="btn-send-report">Invia una Segnalazione</a>

    <div class="access-code-section">
        <p>Digita qui il codice che hai ottenuto da una precedente segnalazione per accedervi:</p>
        <form asp-controller="Segnalazione" asp-action="Access" class="access-code-form" method="post">
            <input type="text" class="access-code-input" placeholder="Codice Segnalazione" name="codice" />
            <button type="submit" class="btn-send-code">Invia</button>
        </form>
    </div>

    @if (!string.IsNullOrEmpty(TempData["CodiceError"] as string))
    {
        <script type="text/javascript">
            window.onload = function () {
                var errorMessage = '@TempData["CodiceError"]';
                alert(errorMessage);
            };
        </script>
    }

    <div class="pdf-section">
        <span>Se vuoi saperne di più sul whistleblowing, clicca qui:</span>
        <a href="@Url.Action("DownloadPDF", "Home")" class="btn-pdf">PDF</a>
    </div>
</main>
